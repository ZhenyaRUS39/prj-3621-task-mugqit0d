# Plan: prj-3621-task-mugqit0d

**Project:** `PRJ-9234`  
**Task ID:** `task-mugqit0d`  
**Repo:** `prj-3621-task-mugqit0d`  

## Summary

Декомпозирую задачу на 5 этапов: структура cargo-проекта, лексер, парсер, сериализация AST, e2e тесты на корректных и edge-case Markdown.

## Subtasks

### 1. Инициализировать cargo-проект и структуру

- **ID:** `sub-1`
- **Profile:** `20razrab1`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Создать Cargo.toml (edition2021), src/lib.rs, src/main.rs (CLI бинарь), директории src/lexer, src/parser, src/ast. Добавить базовые зависимости (thiserror для ошибок, ничего лишнего). Заложить публичный API: pub fn parse(input: &str) -> Result<Document, ParseError>.
- **Test plan:** cargo build проходит; cargo new + cargo init-эквивалент; в src/lib.rs видна сигнатура parse() -> Result<Document, ParseError>.

### 2. Определить AST (структуры Document/Block/Inline)

- **ID:** `sub-2`
- **Profile:** `20razrab1`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Описать типы: Document { blocks: Vec<Block> }, Block enum (Heading{level, text}, Paragraph(Vec<Inline>), CodeBlock{lang, content}, List{ordered, items}, BlockQuote(Vec<Block>), Hr), Inline enum (Text(String), Emphasis(Vec<Inline>), Strong(Vec<Inline>), Code(String), Link{text, url}, Image{alt, url}). Покрыть Debug/Clone/PartialEq для всех.
- **Test plan:** cargo build; cargo doc собирается без warning; типы видны из lib.rs; тесты в src/ast на equality узлов проходят.
- **Dependencies:** `sub-1`

### 3. Реализовать лексер (посимвольный сканер токенов)

- **ID:** `sub-3`
- **Profile:** `30razrab2`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** src/lexer/mod.rs: enum Token { Heading(level), Text, Em, Strong, CodeInline, CodeBlockFence(lang), LinkStart, LinkEnd(url), ImageStart, ImageEnd(url,alt), ListMarker(ordered), BlockQuoteMarker, Hr, Newline, Eof }. Сканер идёт по строкам, эмитит токены, корректно обрабатывает экранирование \* и код-блоки ``` .
- **Test plan:** cargo test lexer -- проходит; unit-тесты на '# Header', '**bold**', '*em*', '`code`', '[txt](url)', '![alt](url)', '```rust\n...```', escape \*.
- **Dependencies:** `sub-2`

### 4. Реализовать парсер (recursive descent)

- **ID:** `sub-4`
- **Profile:** `30razrab2`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** src/parser/mod.rs: recursive descent парсер токенов в AST. Обработать вложенность (strong > em), списки с2+ пробелами отступа, code-block до закрывающего ```, blockquote (каждая строка начинается с '>'), hr (--- или ***).
- **Test plan:** cargo test parser --; e2e: '# H1\n\n**bold _mix_** [a](b)' -> корректный AST; multi-paragraph список; пустой input -> пустой Document.
- **Dependencies:** `sub-3`

### 5. Тесты на реальных Markdown-документах + CLI

- **ID:** `sub-5`
- **Profile:** `20razrab1`
- **Tester:** `TBD`
- **Status:** `pending`
- **Description:** Добавить integration-тесты в tests/ на3+ реальных фикстурах (commonmark-spec minimal subset, README-пример, edge-cases: nested lists, mixed emphasis, trailing newlines). CLI src/main.rs: принимает путь к .md, печатает структуру AST в debug-формате. README с примерами.
- **Test plan:** cargo test --all-targets зелёный; cargo run -- test.md печатает AST; все фикстуры парсятся в ожидаемый AST (snapshot-тесты).
- **Dependencies:** `sub-4`


---
*Generated by Hermes Orchestrator*
